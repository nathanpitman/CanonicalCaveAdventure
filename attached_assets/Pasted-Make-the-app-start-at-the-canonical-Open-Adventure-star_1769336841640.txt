Make the app start at the canonical Open Adventure start scene produced by the importer, without hardcoding "chasm_base".

Context:
- tools/importOpenAdventure.ts now generates client/data/generatedStory.ts and exports:
  export const START_SCENE_ID: string
  along with SCENES / ITEMS / INTRO_MESSAGES / HELP_TEXT.
- client/hooks/useGame.ts imports SCENES/ITEMS/INTRO_MESSAGES/HELP_TEXT from "@/data/story".
- client/data/gameState.ts currently hardcodes initialGameState.sceneId and visitHistory.

TASK (minimal changes):
1) Update client/data/story.ts so it re-exports START_SCENE_ID from generatedStory.ts in addition to the existing exports.
   Example:
     export * from "./generatedStory";
   (This should include START_SCENE_ID automatically if generatedStory exports it.)

2) Update client/data/gameState.ts:
   - Import START_SCENE_ID from "@/data/story"
   - Set initialGameState.sceneId = START_SCENE_ID
   - Set initialGameState.visitHistory = [START_SCENE_ID]
   - Keep previousSceneId null, inventory empty, flags empty, light 100, turns 0, removedActions {} unchanged.

   IMPORTANT: This import must not cause circular dependency issues. If it does:
   - Create a tiny file client/data/startScene.ts that re-exports START_SCENE_ID from generatedStory.ts:
       export { START_SCENE_ID } from "./generatedStory";
     Then import from "@/data/startScene" in gameState.ts instead of importing from story.ts.

3) Update client/hooks/useGame.ts in two places where it assumes "chasm_base":
   A) In handleNewGame(), the initial scene narration currently uses:
        text: SCENES.chasm_base.description
      Replace with:
        text: SCENES[START_SCENE_ID].description
      (import START_SCENE_ID from "@/data/story" or "@/data/startScene" per step 2)

   B) In the initGame() effect where no save exists, the initial scene narration also uses:
        text: SCENES.chasm_base.description
      Replace with:
        text: SCENES[START_SCENE_ID].description

4) Ensure the rest of the game logic remains unchanged:
   - Movement/actions still use SCENES
   - Completion should be triggered by canonical story actions (setsFlag "escaped") not hardcoded scene ids.

VALIDATION:
- Run the app with no save present (delete AsyncStorage save or use Restart Story).
- Confirm the first room shown is the canonical Open Adventure start (surface / end of road / building area depending on YAML).
- Confirm the minimap starts with that scene and visitHistory includes it.