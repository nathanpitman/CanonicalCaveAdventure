I have an Expo React Native + TypeScript game. Do NOT rewrite the app. Only implement an importer that generates content (SCENES/ITEMS) from Open Adventure YAML, and make minimal changes so navigation still works.

My existing files:
- client/data/story.ts defines Action/Scene/Item and exports ITEMS and SCENES.
- client/hooks/useGame.ts imports { SCENES, ITEMS, INTRO_MESSAGES, HELP_TEXT, Action } from "@/data/story"
- client/data/gameState.ts sets initialGameState.sceneId = "chasm_base" and visitHistory starts with ["chasm_base"].

HARD CONSTRAINT:
The game must always have a SCENES["chasm_base"] scene or the app breaks.

GOAL:
1) Create a generator script tools/importOpenAdventure.ts that reads Open Adventure’s adventure.yaml and generates:
   client/data/generatedStory.ts
2) Modify client/data/story.ts to re-export from generatedStory.ts (or export types + re-export generated consts).
3) Ensure generated content matches my story.ts interfaces exactly:
   - export interface Action
   - export interface Scene
   - export interface Item
   - export const ITEMS: Record<string, Item>
   - export const SCENES: Record<string, Scene>
   - export const INTRO_MESSAGES, HELP_TEXT
4) Extend useGame.ts minimally so movement supports directions beyond NSEW (up/down/in/out, optional diagonals).

IMPORTANT BEHAVIOR:
- useGame.handleDirection() currently maps direction words to action ids like go_north. We will generate move actions with those ids.
- parseCommand currently supports only n/s/e/w. We must extend it to accept up/down/in/out.

IMPLEMENTATION:

A) YAML dependency + runner
- Add dependency: "yaml" (preferred) or "js-yaml"
- Add dev dependency: "tsx"
- package.json script:
  "import:oa": "tsx tools/importOpenAdventure.ts"

B) Locate YAML
- Search the workspace for "adventure.yaml". If open-adventure-master is still zipped, extract it first then search again.
- Use the main adventure.yaml.

C) Generator script: tools/importOpenAdventure.ts
- Read YAML via fs.
- Parse it.
- Log top-level keys and sample structures if needed.
- Extract:
  1) Rooms/locations with:
     - room id
     - room short name/title (if present)
     - room long description (if present)
     - exits/travel directions -> destination room ids
  2) Objects/items with:
     - item id
     - item name/description (if present)
     - initial location / where it starts

D) IMPORTANT: Add synthetic ASCENT start scene
Always inject SCENES["chasm_base"] regardless of what YAML contains.

- chasm_base should be like my existing scene:
  id: "chasm_base"
  title: "Base of the Chasm"
  description: (use my existing intro description with lamp mention OR a short one)
  descriptionWithoutItems: similar
  itemDescriptions: optionally include "lamp" if lamp starts here for ASCENT
  actions:
    - { id:"look", label:"LOOK", type:"command", command:"look" }
    - at least one move action: GO EAST to the canonical Open Adventure start room (or whichever room you choose as the first real room)
  items: optionally include lamp (if you want lamp found at start)

Choose the canonical start room as follows:
- If YAML provides a start location, use it.
- Else choose a sensible known early room (first in list).
Store its id as startRoomId.
Then chasm_base.go_east.to = startRoomId

E) Direction/action id mapping
When generating exits, create move Actions like:
- north -> { id:"go_north", label:"GO NORTH", type:"move", to:<dest> }
- south -> go_south
- east -> go_east
- west -> go_west
- up -> go_up
- down -> go_down
- in -> go_in
- out -> go_out
Optional: ne/nw/se/sw -> go_ne etc.

NOTE: If YAML includes directions outside these, ignore them for now but log them.

F) Scene generation format (must match my interfaces)
For each generated room scene:
Scene = {
  id,
  title,
  description,
  descriptionWithoutItems: description (safe default),
  itemDescriptions: { [itemId]: "You see <ItemName> here." } (only for items initially in room),
  actions: [
    { id:"look", label:"LOOK", type:"command", command:"look" },
    ...move actions for exits...
    ...take actions for each initial item...
  ],
  items: [itemIds...]
}

TAKE ACTION format:
{ id:`take_${itemId}`, label:`TAKE ${ITEMNAME_UPPER}`, type:"event", addsItem:itemId, removesAction:true }

G) Items format (must match my Item interface)
Item = { id, name, description, usable, useEffect? }

- Most items usable=false.
- For oil/fuel item:
  usable=true
  useEffect.lightBonus=30
  useEffect.message="You refill the lamp. The flame burns brighter."
- If YAML doesn’t give a description, generate a simple one:
  "<Name>."

H) Modern display names (optional but desired)
Change Item.name (display only) for these ids if present:
- lamp -> "Industrial Lamp"
- oil/fuel -> "Lamp Fuel"
- food -> "Rations"
- bottle -> "Water Bottle"
Do NOT change ids.

I) story.ts export strategy
Modify client/data/story.ts to either:
1) export * from "./generatedStory"
OR
2) keep type interfaces in story.ts, but re-export consts from generatedStory:
   export { ITEMS, SCENES, INTRO_MESSAGES, HELP_TEXT } from "./generatedStory";
Make sure useGame.ts still compiles.

J) Minimal useGame.ts changes
Update handleDirection() dirMap to include:
up/down/in/out and abbreviations u/d.
Also update movePatterns and directDirections in parseCommand to accept:
up/down/in/out/u/d.

Additionally: remove or revise hardcoded win trigger:
In handleMove(), it currently checks if (toSceneId === "exit_slope") then sets escaped.
Once we import YAML, "exit_slope" probably won’t exist.
Replace this with:
- If the target scene has an action with setsFlag === "escaped" OR
- Add a synthetic "escape" event scene later.
For now simplest: remove the special-case win check so navigation works without auto-winning.

K) Validation
At end of generator:
- Log scene count and item count
- Assert SCENES has "chasm_base"
- Assert the chosen startRoomId exists in SCENES
- Print startRoomId + its exits count
Expect scene count > 100

After implementation:
- Run npm run import:oa
- Run Expo
- Confirm the app loads at chasm_base and GO EAST enters the imported world
- Confirm LOOK works, movement works, TAKE works in at least one room