We successfully imported Open Adventure YAML into generatedStory.ts and injected a synthetic "chasm_base" start scene. However, right now chasm_base routes into the original surface start area (the brick building).

NEW DESIGN REQUIREMENT:
ASCENT is an escape game. Player starts at the deepest part of the cave network, and must work upwards to reach the surface exit gate. Therefore:
1) Relocate the connection from chasm_base so it links into a DEEP location in the Open Adventure graph (not the surface building).
2) Ensure the item dependency chain remains viable in the reversed structure: any item required to pass a gate/hazard must be obtainable before the player encounters that gate/hazard on the escape route.

IMPORTANT CONSTRAINTS:
- Keep the app UI and useGame logic unchanged.
- Continue using generatedStory.ts and the generator script.
- chasm_base must still exist (gameState.ts hardcodes it).
- Changes should be implemented in tools/importOpenAdventure.ts (generation stage), not runtime.

PART 1 — START RELOCATION (deep start)
A) Add a generator configuration section at the top of tools/importOpenAdventure.ts:
   const ASCENT_MODE = true;
   const ASCENT_START_ROOM_ID = "<to be selected>";
   const ASCENT_EXIT_ROOM_ID = "<to be selected>";
B) Implement logic to choose a deep start room automatically if ASCENT_START_ROOM_ID is not set:
   1) Identify candidate rooms that are "deep". Use heuristics:
      - Rooms whose title/description contains keywords like: "cave", "cavern", "chamber", "maze", "pit", "hall", "bedquilt", "deep", "complex", "end of", "low", "below", "y2", etc.
      - Exclude obvious surface/near-surface rooms: anything mentioning "building", "forest", "road", "valley", "streambed", "outside", "house", "end of road".
   2) From candidates, compute graph distance from the canonical surface/building region:
      - Find the canonical surface node(s) by keyword "building" or by known start.
      - Run BFS shortest path lengths.
      - Choose a room in the top 5% farthest distance as deep start.
   3) Log which room you chose and why, and allow overriding via ASCENT_START_ROOM_ID.
C) Update synthetic chasm_base scene so:
   - it has a move action that goes to ASCENT_START_ROOM_ID (e.g., GO EAST)
   - it does NOT route to the building start area
   - chasm_base description remains ASCENT-specific.

PART 2 — DEFINE ESCAPE EXIT (surface goal)
A) In ASCENT mode, define ASCENT_EXIT_ROOM_ID similarly:
   - Identify a surface exit node (keyword: building/road/outside/forest/end of road).
   - This is where the rescue gate / final grate will be.
B) Do NOT auto-win on entering some old scene id like exit_slope. Instead:
   - Add a special event action "escape" only on ASCENT_EXIT_ROOM_ID:
     { id:"escape", label:"UNLOCK THE GATE", type:"event", requiresItem:"rescue_key", setsFlag:"escaped" }
   - Also generate a "rescue_key" item (if not present in YAML) as a non-usable item.
   - Update ITEMS to include rescue_key with display name "Rescue Gate Key".
   - Ensure HELP_TEXT mentions you must find a key to escape.
C) Ensure useGame detects escaped via flags as it already does.

PART 3 — DEPENDENCY VIABILITY (avoid deadlocks after reversal)
We must ensure that on the path from ASCENT_START_ROOM_ID to ASCENT_EXIT_ROOM_ID, any required items appear BEFORE their first required use.

Implement a simple viability pass in the generator:

1) Build a directed or undirected graph of rooms (use undirected for reachability).
2) Compute at least one likely escape route:
   - Use BFS from start to exit to get a shortest path list of rooms (pathRooms[]).
3) Identify "gates" that require items:
   - For now, approximate gates using YAML metadata if it exists OR a hardcoded list of canonical critical items:
     lamp, fuel/oil, bottle, food, cage, bird, rod(s), axe, keys/rescue_key
   - Since our current RN prototype doesn't implement full canonical gating logic, we only ensure items are available in sensible order.
4) Implement a rule-based placement for ASCENT mode:
   - Place lamp in chasm_base (found at chasm base).
   - Place fuel/oil within the next N rooms on the path (e.g., within first 10 rooms).
   - Place bottle within first 20 rooms on the path (after first encounter of water-themed room keyword "stream"/"water").
   - Place food within first 25 rooms.
   - Place cage BEFORE bird, both before serpent/hazard region (use keyword heuristics to detect serpent/hazard rooms; if not found, just place cage in early-mid and bird in mid).
   - Place rescue_key in late-upper region: within last 15% of pathRooms but not in the exit room itself.
5) Implementation detail:
   - Do not delete original YAML placements globally. Instead, in ASCENT mode create "cache" placements by ADDING these critical items into chosen rooms along the escape path if they are not already earlier.
   - This guarantees viability without needing full puzzle logic.
6) Log a "dependency report" at generation time:
   - start room id and title
   - exit room id and title
   - for each critical item: which room it was placed in and its index along path
   - warn if any item index occurs after its intended milestone

PART 4 — KEEP APP COMPATIBILITY
- Ensure SCENES still includes chasm_base.
- Ensure action ids match useGame’s direction mapping (go_north etc).
- Ensure take actions are generated for newly placed cache items.
- Ensure ITEMS contains all added items.

DELIVERABLE:
- Modify tools/importOpenAdventure.ts to implement ASCENT_MODE relocation + cache placement + exit event + dependency report.
- Regenerate client/data/generatedStory.ts and confirm the app starts at chasm_base and routes into a deep cave room (not the building).